<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>card-position</title>
  <style>
  
  /* 기본 세팅 */
body {
  background-color: #f4f4f4;
  font-family: sans-serif;
}

/* 1. 부모 컨테이너 (기준점) */
.card-container {
  width: 400px;
  height: 500px;
  background: white;
  margin: 50px auto;
  border-radius: 15px;
  overflow: hidden;
  box-shadow: 0 10px 20px rgba(0,0,0,0.1);
  
  /* 중요: 자식들의 absolute 배치를 위한 기준점 설정 */
  position: relative; 
}

/* 2. 상단 배너 */
.banner {
  height: 200px;
  background: linear-gradient(45deg, #997dd6, #211eae);
}

/* 3. 프로필 이미지-중앙 배치  */
.profile-img {
  width: 130px;
  height: 130px;
  background: #923f3f;
  border: 4px solid white;
  border-radius: 50%;
  
  /* 기준점으로부터 배치 */
  position: absolute;
  top: 200px;       /* 배너 바로 아래 지점 */
  left: 50%;        /* 일단 가로 50% 지점으로 이동 */
  transform: translate(-50%, -50%); /* 중앙 정렬 */
}
.profile-img img {
  width: 100%;
  height: 100%;
  border-radius: 50%;
}

/* 4. 정보 텍스트 */
.info {
  width: 400px;
  height: 130px;
  text-align: center;
  position: relative;  
  /*border: 1px solid ;*/
  box-sizing: border-box;
}
.info h3 {
  position: absolute;
  top: 50px;
  left: 50%;
  transform: translateX(-50%);
}
.info p {
  position: absolute;
  top: 80px;
  left: 50%;
  transform: translateX(-50%);
}

/* 5. 우측 상단 배지 */
.badge {
  position: absolute;
  top: 15px;
  right: 15px;
  background: #4caf50;
  color: white;
  padding: 5px 10px;
  font-size: 14px;
  border-radius: 20px;
}

/* 6. 화면 하단 고정 버튼 */
.fixed-btn {
  position: fixed; /* 브라우저 창 기준 고정 */
  bottom: 20px;
  right: 20px;
  padding: 15px 25px;
  background: #333;
  color: white;
  border: none;
  border-radius: 30px;
  cursor: pointer;
  box-shadow: 0 5px 10px rgba(0,0,0,0.2);
}
</style>
</head>
<body>
  <div class="card-container">
  <div class="banner"></div>
  
  <div class="profile-img">
    <img src="../images/profile.jpg" alt="">
  </div>
  
  <div class="info">
    <h3>PROMPT User</h3>
    <p>Position Master</p>
  </div>

  <span class="badge">Online</span>
</div>

<button class="fixed-btn">문의하기</button>
</body>
</html>