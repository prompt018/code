<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>웹 컴포넌트 패키지</title>
  <style>
    /* 기본 설정 */
body { 
  background-color: #f8f9fa; 
  font-family: sans-serif; 
  padding: 50px; 
}
.container { 
  width: 600px; 
  margin: 0 auto; 
  text-align: center; 
}
h2 { 
  margin-bottom: 40px; 
  color: #333; 
}

/* 공통 트랜지션 설정: 속성시간 / 시간 / 효과 */
/* transition: all 0.3s ease-in-out; */

/* 1. 크기 변형 (Transform & Transition) */
.zoom-card {
  width: 200px;
  height: 100px;
  background-color: #3498db;
  margin: 20px auto;
  color: white;
  line-height: 100px;
  border-radius: 10px; 
  transition: transform 0.3s ease, background-color 0.3s ease;  /* 변화를 부드럽게 만듦 */
}

.zoom-card:hover {
  transform: scale(1.1); /* 1.1배 커짐 */
  background-color: #2980b9;
  cursor: pointer;
}

/* 2. 배경색과 테두리 변형 */
.color-button {
  display: inline-block;
  padding: 15px 30px;
  background-color: transparent;
  border: 2px solid #e74c3c;
  color: #e74c3c;
  font-weight: bold;
  margin: 20px 0;
  cursor: pointer;
  /* 배경색과 글자색 변화에 트랜지션 적용 */
  transition: all 0.4s;
}

.color-button:hover {
  background-color: #e74c3c;
  color: white;
  border-radius: 30px; /* 각진 버튼이 동그랗게 변함 */
}

/* 3. 위치 이동 (Margin-left 활용) */
.slide-container {
  width: 100%;
  height: 50px;
  background-color: #eee;
  margin: 20px 0;
  text-align: left; /* 내부 박스 왼쪽 정렬 */
}

.sliding-box {
  width: 100px;
  height: 50px;
  background-color: #2ecc71;
  color: white;
  line-height: 50px;
  text-align: center;
  /* 위치 변화에 트랜지션 적용 */
  transition: margin-left 0.5s cubic-bezier(0.68, -0.55, 0.27, 1.55);
}

.slide-container:hover .sliding-box {
  margin-left: 450px; /* 마우스를 올리면 오른쪽으로 슬라이드 */
}

/* 4. 투명도와 Position 활용 (오버레이 효과) */
.overlay-card {
  width: 300px;
  height: 200px;
  margin: 40px auto;
  background-color: #333;
  position: relative;
  overflow: hidden;
}

.base-image {
  width: 100%;
  height: 100%;
  line-height: 200px;
  color: #666;
  font-size: 24px;
}

.base-image img {
  width: 100%;
  height: 100%;
}

.overlay-text {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0,0,0,0.7);
  color: white;
  line-height: 200px;
  /* 초기 상태: 투명하고 아래에 숨어 있음 */
  opacity: 0;
  transform: translateY(100%);
  transition: all 0.4s ease-out;
}

.overlay-card:hover .overlay-text {
  opacity: 1;
  transform: translateY(0); /* 위로 스윽 올라오며 나타남 */
}

.box-container {
  height: 200px;
  background-color: #f1f2f6;
  position: relative;
}

.moving-box {
  width: 100px;
  height: 100px;
  background-color: #2ed573;
  color: white;
  line-height: 100px;
  text-align: center;
  transition: 0.5s;
}

/* 마우스를 컨테이너에 올리면 박스가 오른쪽과 아래로 이동 */
.box-container:hover .moving-box {
  /* X축으로 50px, Y축으로 20px 이동 */
  transform: translate(50px, 20px);
}
  </style>
</head>
<body>
  <div class="container">
        <h2>마우스를 올려보세요 (Hover Me!)</h2>

        <div class="zoom-card">
            <div class="box-label">Scale Up</div>
        </div>

        <div class="color-button">
            색상 변경 버튼
        </div>

        <div class="slide-container">
            <div class="sliding-box">Slide</div>
        </div>

        <div class="box-container">
          <div class="moving-box">Move!</div>
        </div>

        <div class="overlay-card">
            <div class="base-image">
              <img src="../images/content1.jpg" alt="">
            </div>
            <div class="overlay-text">상세 정보 보기</div>
        </div>
    </div>
</body>
</html>