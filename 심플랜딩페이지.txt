<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <style>
        body { margin: 0; background-color: #e9ecef; font-family: 'Segoe UI', sans-serif; }

        /* [1] 기준점이 되는 메인 카드 */
        .card {
            position: relative;
            width: 350px;
            height: 500px;
            background: white;
            margin: 100px auto;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            padding: 20px;
            box-sizing: border-box;
        }

        /* [2] 이미지 박스 - 박스 밖으로 살짝 튀어나오는 효과 */
        .image-container {
            position: relative;
            width: 100%;
            height: 250px;
            background: #333;
            border-radius: 15px;
            margin-top: -50px; /* 위로 50px만큼 강제 이동 (absolute 없이도 가능하지만 겹침 효과를 위해) */
            box-shadow: 0 10px 20px rgba(0,0,0,0.2);
            overflow: hidden;
            background-image: url(https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQlhnAB9offT0OlgGB5PrgUz0n8ye7JhLqYTw&s);
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
        }

        /* [3] 이미지 위의 'Sale' 라벨 - 회전 배치 */
        .sale-label {
            position: absolute;
            top: 15px;
            right: -30px; /* 오른쪽 밖으로 살짝 밀기 */
            background: #ff3e3e;
            color: white;
            padding: 5px 40px;
            transform: rotate(45deg); /* 요소를 회전시킴 */
            font-size: 12px;
            font-weight: bold;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
        }

        /* [4] 이미지 정중앙에 위치한 'Quick View' 아이콘 */
        .quick-view {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%); /* 정확한 중앙 정렬 공식 */
            background: rgba(255, 255, 255, 0.9);
            padding: 10px 20px;
            border-radius: 30px;
            font-size: 13px;
            opacity: 0; /* 평소엔 숨김 */
            transition: 0.3s;
        }

        .image-container:hover .quick-view {
            opacity: 1; /* 마우스 올리면 나타남 */
        }

        /* [5] 카드 구석의 하트 버튼 */
        .like-btn {
            position: absolute;
            top: 240px; /* 이미지 하단 경계선 */
            right: 40px;
            width: 45px;
            height: 45px;
            background: #ff3e3e;
            border-radius: 50%;
            color: white;
            line-height: 45px;
            text-align: center;
            font-size: 20px;
            cursor: pointer;
            border: 4px solid white; /* 카드 배경과 분리되는 느낌 */
        }

        /* [6] 하단 고정 가격표 */
        .price-tag {
            position: absolute;
            bottom: 20px;
            left: 20px;
            right: 20px;
            border-top: 1px dashed #ddd;
            padding-top: 15px;
        }

        .original-price {
            position: absolute;
            right: 0;
            top: 15px;
            text-decoration: line-through;
            color: #383737;
            font-size: 14px;
        }
    </style>
</head>
<body>

    <div class="card">
        <div class="image-container">
         
            <div class="sale-label">SALE 20%</div>
            <div class="quick-view">미리보기</div>
        </div>

        <div class="like-btn">♥</div>

        <div class="content">
            <h2 style="margin-top:40px;">Vintage Camera</h2>
            <p style="color:#666; font-size: 14px; line-height: 1.6;">
                세월의 흔적이 고스란히 담긴 빈티지 카메라입니다. 
                인테리어 소품으로도, 실제 촬영용으로도 훌륭합니다.
            </p>
        </div>

        <div class="price-tag">
            <span style="font-size: 24px; font-weight: bold; color: #333;">￦240,000</span>
            <span class="original-price">￦300,000</span>
        </div>
    </div>

</body>
</html>